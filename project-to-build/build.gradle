plugins {
    id 'java'
}

repositories {
    maven {
        url 'http://archiva:8080/repository/snapshots'
        credentials {
            username System.getenv('ARCHIVA_USR')
            password System.getenv('ARCHIVA_PSW')
        }
    }
}

dependencies {
    implementation group: 'com.tom', name: 'artifact-to-publish', version: project.version
}